import styled from "styled-components";

export const DeleteModalWindow = styled.div(({ direct }) => `
display: block;
width: 400px;
height: 200px;
margin: auto;
background: #FCFCFC;
border-radius: 10px;

transition: stroke 250ms linear, rotate 200ms linear;
`);